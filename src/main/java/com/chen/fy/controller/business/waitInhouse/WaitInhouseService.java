package com.chen.fy.controller.business.waitInhouse;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

import com.chen.fy.controller.business.commission.purchase.PurchaseService;

public class WaitInhouseService {
	public final static WaitInhouseService me = new WaitInhouseService();
	private static final Logger logger = LogManager.getLogger(PurchaseService.class);
}
